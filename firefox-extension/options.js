// SPDX-License-Identifier: MPL-2.0
const input=document.querySelector('#instance'),instanceId=document.querySelector('#instance-id'),pair=document.querySelector('#pair'),origin=document.querySelector('#origin'),status=document.querySelector('#status'),sites=document.querySelector('#sites'),images=document.querySelector('#images'),videos=document.querySelector('#videos');
const render=async()=>{const s=await browser.storage.local.get(['instanceUrl','instanceId','pairId','sites']);input.value=s.instanceUrl||'';instanceId.value=s.instanceId||'';pair.value=s.pairId||'';sites.textContent='';(s.sites||[]).forEach(rule=>{const li=document.createElement('li');li.textContent=`${rule.origin}: capture ${rule.capture?'on':'paused'}, substitution ${rule.substitution?'on':'paused'}, ${rule.media.join('/')}`;for(const [label,key] of [['Toggle capture','capture'],['Toggle substitution','substitution'],['Remove','remove']]){const button=document.createElement('button');button.textContent=label;button.onclick=async()=>{if(key==='remove'){await browser.permissions.remove({origins:[`${rule.origin}/*`]});s.sites=s.sites.filter(item=>item.origin!==rule.origin)}else{rule[key]=!rule[key]}await browser.storage.local.set({sites:s.sites});render()};li.append(' ',button)}sites.append(li)});};render();
document.querySelector('#save').onclick=async()=>{let value;try{value=new URL(input.value.trim()).origin}catch{status.textContent='HTTPS URL, instance identifier, and pairing reference required.';return}if(!value.startsWith('https://')||!instanceId.value.trim()||!pair.value.trim()){status.textContent='HTTPS URL, instance identifier, and pairing reference required.';return}const granted=await browser.permissions.request({origins:[`${value}/*`]});if(!granted){status.textContent='Permission for the paired x-img instance was not granted.';return}await browser.storage.local.set({instanceUrl:value,instanceId:instanceId.value.trim(),pairId:pair.value.trim()});status.textContent='Paired. The opaque reference is bound to the host session; no site password or site cookie is stored.';};
document.querySelector('#enable').onclick=async()=>{let value;try{value=new URL(origin.value).origin}catch{status.textContent='Enter an HTTPS origin.';return}if(!value.startsWith('https://')){status.textContent='Only HTTPS origins are supported.';return}const granted=await browser.permissions.request({origins:[`${value}/*`]});if(!granted){status.textContent='Permission was not granted.';return}const s=await browser.storage.local.get('sites');const rule={origin:value,capture:true,substitution:false,media:[...(images.checked?['images']:[]),...(videos.checked?['videos']:[])]};await browser.storage.local.set({sites:[...(s.sites||[]).filter(item=>item.origin!==value),rule]});status.textContent='Enabled with explicit capture/substitution policy.';render();};
